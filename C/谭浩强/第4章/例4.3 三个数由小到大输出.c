#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<stdlib.h>
#include<math.h>
void main(){
	/*

	s1:if a>b 将a和b交换，(交换后，a是a,b中的小者)
	s2:if a>c 将a和c交换，（交换后,a是a,c中的小者，因此a是三者中的最小者）
	s3:if b>c 将b和c交换， （交换后，b是b，c中的小者，也是三者中的次小者）

	*/
	system("pause");
}